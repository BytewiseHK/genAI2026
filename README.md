# genAI2026

AI ambassador programme at HKBU.

## Running the project

- **Locally:** `npm install` then `npm run dev` (see repo root for scripts).
- **On Replit:** See the [replit/](./replit/) folder (and [replit/REPLIT.md](./replit/REPLIT.md)) for the live link and setup.
